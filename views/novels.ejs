<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <header>
        <h1>
            <%= title %>
        </h1>
        <a href="/">Home</a>
    </header>
    <main>
        <section>
            <h2>Filter Novels</h2>
            <form method="GET" action="/novels">
                <input type="text" name="genre" placeholder="Filter by genre" value="<%= genreFilter || '' %>">
                <button type="submit">Filter</button>
                <% if (genreFilter) { %>
                    <a href="/novels">Clear Filter</a>
                    <% } %>
            </form>
        </section>
        <section>
            <h2>All Novels</h2>
            <ul>
                <% novels.forEach(novel=> { %>
                    <li>
                        <h3>
                            <%= novel.title %>
                        </h3>
                        <p>Author: <%= novel.author %>
                        </p>
                        <p>Genre: <%= novel.genre %>
                        </p>
                        <a href="/novels/<%= novel.id %>">View Details</a>
                        <form action="/novels/<%= novel.id %>?_method=DELETE" method="POST" style="display: inline;">
                            <button type="submit">Delete</button>
                        </form>
                    </li>
                    <% }); %>
            </ul>
        </section>
    </main>
</body>

</html>